#pragma once
// bookkeeping.h - Bookkeeping module
// Handles income/expense records and category management

#include <string>

namespace Bookkeeping {
    // Enter the bookkeeping submenu (main entry)
    void run(const std::string& userId);
}
